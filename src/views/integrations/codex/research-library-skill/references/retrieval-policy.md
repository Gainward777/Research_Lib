# Retrieval policy

1. Search before starting research on an existing topic.
2. Prefer `synthesize=false` for cheap retrieval.
3. Use `ask` only when a source-grounded synthesis is needed.
4. Cite `library_item_id` values in the final report.
